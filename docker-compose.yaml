services:
  hydra-saml-auth:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: hydra-saml-auth
    env_file:
      - .env
    command: ["npm", "start"]
    ports:
      - "6969:6969"
    restart: unless-stopped
